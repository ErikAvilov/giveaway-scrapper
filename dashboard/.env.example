# Server-only Neon pooled connection string.
# NEVER prefix with NEXT_PUBLIC_ — this must not reach the browser bundle.
DATABASE_URL=postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require
